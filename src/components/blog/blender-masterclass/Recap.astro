---
interface Props {
  title?: string;
  points: string[];
}
const { title = 'YOU BUILT', points } = Astro.props;
---

<div class="recap">
  <div class="head">✓ {title}</div>
  <ul>
    {points.map((p) => <li>{p}</li>)}
  </ul>
</div>
